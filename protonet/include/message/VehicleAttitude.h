/**@file This file was autogenerated. All changes will be undone. */

/** Message: Vehicle_Attitude, ID: 2004*/

#ifndef _VEHICLE_ATTITUDE_H_
#define _VEHICLE_ATTITUDE_H_

#include <protonet_marshal.h>
#include <protonet_message.h>

typedef struct {
   float64_t timestamp;
   uint16_t vehicle_ID;
   float32_t roll;
   float32_t pitch;
   float32_t yaw;
} vehicle_attitude_t;

msg_offset pack_vehicle_attitude(
   vehicle_attitude_t* vehicle_attitude,
   msg_offset offset);

msg_offset unpack_vehicle_attitude(
   msg_offset offset,
   vehicle_attitude_t* out_ptr);

void encode_vehicle_attitude(
   uint8_t src_id,
   uint8_t dest_id,
   uint8_t msg_ttl,
   uint8_t seq_number,
   vehicle_attitude_t* tx_msg,
   proto_msg_t* msg);

#endif