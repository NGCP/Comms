For some reason linux cmake will fail once

Ex:
$cmake "Unix Makefile" ../../
$make 

fails!!!

just doit again

$cmake "Unix Makefile" ../../
$make

which will compile has to do with an issue with random class templates
