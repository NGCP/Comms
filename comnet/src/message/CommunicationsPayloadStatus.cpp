/* This file was autogenerated. All changes will be undone. */

/* Message: Communications_Payload_Status, ID: 4003*/

#include <comnet_marshal.h>
#include <comnet_message.h>
#include <message/CommunicationsPayloadStatus.h>

msg_offset pack_communications_payload_status(
   communications_payload_status_t* communications_payload_status,
   msg_offset offset)
{
   offset = pack_float64_t(communications_payload_status->timestamp,offset);
   offset = pack_uint16_t(communications_payload_status->communications_ID,offset);
   offset = pack_uint8_t(communications_payload_status->communications_mode,offset);
   offset = pack_uint8_t(communications_payload_status->communications_status,offset);
   return offset;
}

msg_offset unpack_communications_payload_status(
   msg_offset offset,
   communications_payload_status_t* out_ptr)
{
   offset = unpack_float64_t(offset, &out_ptr->timestamp);
   offset = unpack_uint16_t(offset, &out_ptr->communications_ID);
   offset = unpack_uint8_t(offset, &out_ptr->communications_mode);
   offset = unpack_uint8_t(offset, &out_ptr->communications_status);
   return offset;
}

void encode_communications_payload_status(
   uint8_t src_id,
   uint8_t dest_id,
   communications_payload_status_t* tx_msg,
   com_msg_t* msg,
   uint8_t keyArr[])//key for encryption CryptoPP

{
   /** Random IV*/
   Random::RandomNumberGen randIV = Random::RandomNumberGen();
   randIV.generate_numbers<uint8_t>(msg->header.iv,0, AES::BLOCKSIZE,0,255);

   msg_offset start_offset, offset;
   msg->direction = Com_Out;
   start_offset = msg->data;
   offset = msg->data;
   msg->header.node_src_id = src_id;
   msg->header.node_dest_id = dest_id;
   msg->header.message_length = 12;
   msg->header.message_type = Com_Communications_Payload_Status;
   offset = pack_sync(offset);
   offset = pack_header(&msg->header, offset);

   /** Encrypt */
   /* Managed C (CLR) will freak out if you pass SecByteBlock as an argument this is my half ass fix MW*/
   CryptoPP::SecByteBlock key(keyArr, AES::DEFAULT_KEYLENGTH);
   CFB_Mode<AES>::Encryption cfbEncryption(key, key.size(), msg->header.iv);
   cfbEncryption.ProcessData((byte*)offset, (byte*)offset, msg->header.message_length);

   offset = pack_communications_payload_status(tx_msg, offset);
   offset = pack_checksum(start_offset, offset);
   msg->tx_len = offset-start_offset;
}