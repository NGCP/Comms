/* This file was autogenerated. All changes will be undone. */

/* Message: ROV_Status_Message, ID: 9001*/

#include <comnet_marshal.h>
#include <comnet_message.h>
#include <message/ROVStatusMessage.h>

msg_offset pack_ROV_status_message(
   ROV_status_message_t* ROV_status_message,
   msg_offset offset)
{
   offset = pack_float64_t(ROV_status_message->timestamp,offset);
   offset = pack_uint16_t(ROV_status_message->vehicle_ID,offset);
   offset = pack_uint16_t(ROV_status_message->depth,offset);
   offset = pack_uint16_t(ROV_status_message->roll,offset);
   offset = pack_uint16_t(ROV_status_message->pitch,offset);
   offset = pack_uint16_t(ROV_status_message->heading,offset);
   offset = pack_float32_t(ROV_status_message->battery_percent,offset);
   return offset;
}

msg_offset unpack_ROV_status_message(
   msg_offset offset,
   ROV_status_message_t* out_ptr)
{
   offset = unpack_float64_t(offset, &out_ptr->timestamp);
   offset = unpack_uint16_t(offset, &out_ptr->vehicle_ID);
   offset = unpack_uint16_t(offset, &out_ptr->depth);
   offset = unpack_uint16_t(offset, &out_ptr->roll);
   offset = unpack_uint16_t(offset, &out_ptr->pitch);
   offset = unpack_uint16_t(offset, &out_ptr->heading);
   offset = unpack_float32_t(offset, &out_ptr->battery_percent);
   return offset;
}

void encode_ROV_status_message(
   uint8_t src_id,
   uint8_t dest_id,
   ROV_status_message_t* tx_msg,
   com_msg_t* msg,
   uint8_t keyArr[])//key for encryption CryptoPP

{
   /** Random IV*/
   Random::RandomNumberGen randIV = Random::RandomNumberGen();
   randIV.generate_numbers<uint8_t>(msg->header.iv,0, AES::BLOCKSIZE,0,255);

   msg_offset start_offset, offset;
   msg->direction = Com_Out;
   start_offset = msg->data;
   offset = msg->data;
   msg->header.node_src_id = src_id;
   msg->header.node_dest_id = dest_id;
   msg->header.message_length = 22;
   msg->header.message_type = Com_ROV_Status_Message;
   offset = pack_sync(offset);
   offset = pack_header(&msg->header, offset);

   /** Encrypt */
   /* Managed C (CLR) will freak out if you pass SecByteBlock as an argument this is my half ass fix MW*/
   CryptoPP::SecByteBlock key(keyArr, AES::DEFAULT_KEYLENGTH);
   CFB_Mode<AES>::Encryption cfbEncryption(key, key.size(), msg->header.iv);
   cfbEncryption.ProcessData((byte*)offset, (byte*)offset, msg->header.message_length);

   offset = pack_ROV_status_message(tx_msg, offset);
   offset = pack_checksum(start_offset, offset);
   msg->tx_len = offset-start_offset;
}