/**@file This file was autogenerated. All changes will be undone. */

/** Message: UAV_Collision_Avodiance_Message, ID: 6006*/

#ifndef _UAV_COLLISION_AVODIANCE_MESSAGE_H_
#define _UAV_COLLISION_AVODIANCE_MESSAGE_H_

#include <comnet_marshal.h>
#include <comnet_message.h>

#include"random_numbers.h"
#include "aes.h"
using CryptoPP::AES;
#include "ccm.h"
using CryptoPP::CFB_Mode;
typedef struct {
   float64_t timestamp;
   uint8_t vehicle_ID;
   float64_t velocity;
   float64_t latitude;
   float64_t longtitude;
   float64_t altitude;
   uint8_t priority;
} UAV_collision_avodiance_message_t;

msg_offset pack_UAV_collision_avodiance_message(
   UAV_collision_avodiance_message_t* UAV_collision_avodiance_message,
   msg_offset offset);

msg_offset unpack_UAV_collision_avodiance_message(
   msg_offset offset,
   UAV_collision_avodiance_message_t* out_ptr);

void encode_UAV_collision_avodiance_message(
   uint8_t src_id,
   uint8_t dest_id,
   UAV_collision_avodiance_message_t* tx_msg,
   com_msg_t* msg,
   uint8_t keyArr[]);//key for encryption CryptoPP

#endif