/**@file This file was autogenerated. All changes will be undone. */

/** Message: Amy_Stuff, ID: 9000*/

#ifndef _AMY_STUFF_H_
#define _AMY_STUFF_H_

#include <comnet_marshal.h>
#include <comnet_message.h>

#include"random_numbers.h"
#include "aes.h"
using CryptoPP::AES;
#include "ccm.h"
using CryptoPP::CFB_Mode;
typedef struct {
   float64_t hello;
} amy_stuff_t;

msg_offset pack_amy_stuff(
   amy_stuff_t* amy_stuff,
   msg_offset offset);

msg_offset unpack_amy_stuff(
   msg_offset offset,
   amy_stuff_t* out_ptr);

void encode_amy_stuff(
   uint8_t src_id,
   uint8_t dest_id,
   amy_stuff_t* tx_msg,
   com_msg_t* msg,
   uint8_t keyArr[]);//key for encryption CryptoPP

#endif